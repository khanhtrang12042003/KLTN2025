@using KLTN2025.DTOs
@model DangNhapDTO;
@{
    ViewData["Title"] = "Đăng nhập";
    ViewData["css"] = "Validation.css";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string? thongBao = ViewBag.ThongBao as string;
}

@{
    if (!string.IsNullOrEmpty(thongBao))
    {
        <div id="alert-message" class="custom-alert">
            @thongBao
        </div>
    }
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow border-0 p-4">
                <h3 class="fw-bold text-center text-primary mb-4">Đăng nhập</h3>

                <form asp-action="DangNhap" method="post">
                    @Html.AntiForgeryToken()
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Tên đăng nhập</label>
                        <input type="text" asp-for="TenDangNhap" class="form-control" />
                        <span class="d-block invalid-feedback" asp-validation-for="TenDangNhap"></span>
                    </div>

                    <div class="mb-3">
                        <label class="form-label fw-semibold">Mật khẩu</label>
                        <input type="password" asp-for="MatKhau" class="form-control" />
                        <span class="d-block invalid-feedback" asp-validation-for="MatKhau"></span>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 py-2">Đăng nhập</button>

                    <div class="text-center mt-3">
                        <small>Chưa có tài khoản? <a asp-action="DangKy"
                                class="text-decoration-none text-primary fw-semibold">Đăng ký ngay</a></small><br />
                        <small><a asp-action="QuenMatKhau" class="text-decoration-none text-secondary">Quên mật
                                khẩu?</a></small>
                    </div>
                </form>
            </div>
        </div>
    </div>
    @section Scripts {
        <script>
            document.addEventListener("DOMContentLoaded", () => {
                // Ẩn các thông báo lỗi khi gõ trong phần input
                document.querySelectorAll("input").forEach(input => {
                    input.addEventListener("input", () => {
                        const msg = document.querySelector(`[data-valmsg-for='${input.name}']`)
                        if (msg) {
                            msg.textContent = "";
                            msg.classList.remove("d-block", "field-validation-error");
                            msg.classList.add("d-none");
                        }
                        input.classList.remove("input-validation-error")
                    })
                })

                const alert = document.getElementById("alert-message");
                if (alert) {
                    setTimeout(() => alert.classList.add("show"), 100);

                    setTimeout(() => {
                        alert.classList.remove("show");
                        setTimeout(() => alert.remove(), 500)
                    }, 5000)
                }
            })
        </script>
    }
</div>